/* Problem 8
Weather Forecast: Build a weather app that informs users about the
weather conditions. If the weather forecast predicts no rain (!isRaining),
display a message indicating that it's a good day for outdoor activities. */

let isRaining=0         //change the value 0 or 1 and check the result.
if (!isRaining) {
    console.log("Good Day for Outdoor Activities.");
} else {
    console.log("Raining");
}